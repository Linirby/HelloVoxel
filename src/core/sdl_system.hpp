/// SDL system initialization wrapper.
#pragma once

namespace lili {

struct SDLSystem {
	/// Initialize SDL systems.
	SDLSystem();
	/// Shut down SDL systems.
	~SDLSystem();
};

}  // namespace lili
